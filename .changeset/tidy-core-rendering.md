---
"@zenuml/core": patch
---

Reduce the core maintenance surface by consolidating Markdown rendering, cloud icon loading, and single-block fragment rendering while preserving parser and diagram behavior. Markdown watch mode now reuses the same rendering path as one-shot mode.
