/**
 * Shared constants used throughout the application.
 * These constants are independent of the parser layer.
 */

/**
 * Special participant identifier for the diagram entry point.
 * Represents the implicit starting participant in sequence diagrams.
 */
export const _STARTER_ = "_STARTER_";