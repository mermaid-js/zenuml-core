/**
 * Title View Model
 * 
 * Represents the title of a sequence diagram with its display text.
 * Used by the DiagramTitle component for rendering diagram titles.
 */

export interface TitleVM {
  /** The title text to display */
  text: string;
}
